import React from 'react';
import { Task, Shift, Event } from "./../../utils/types"

export default function CalendarCard({ id, data }: { id: string, data: Task|Shift|Event}) {
    return (
        <div/>
    );
}